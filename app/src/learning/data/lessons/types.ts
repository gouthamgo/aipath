// Shared types for lesson content

export interface LessonContent {
  slug: string;
  problemContent: string;
  solutionContent: string;
  explanationContent: string;
  realworldContent: string;
  mistakesContent: string;
  interviewContent: string;
  starterCode: string;
  solutionCode: string;
  hints: string[];
}
